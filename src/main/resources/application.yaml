server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:bankDb;
    username: med
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create
logging:
  level:
    root: info
    org:
      hibernate:
        SQL: DEBUG
